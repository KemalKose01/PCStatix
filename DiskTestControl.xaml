<UserControl x:Class="HardwareMonitor.DiskStressTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" d:DesignHeight="500" d:DesignWidth="600">
            
              

    <UserControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Cursor" Value="Hand"/>
            
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="border" CornerRadius="10" Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <StackPanel Margin="20">
            <TextBlock Text="DİSK OKUMA/YAZMA HIZI TESTİ" Foreground="#FFAA00" FontSize="22" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,10"/>
            <TextBlock x:Name="TxtDiskHealth" Text="Disk Durumu: Sağlıklı" Foreground="Gray" FontSize="14" HorizontalAlignment="Center" Margin="0,0,0,20"/>

            <Border Background="#1e1e1e" CornerRadius="15" Padding="20">
                <UniformGrid Columns="2">
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="ANLIK YAZMA" Foreground="#95a5a6" FontSize="10" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtLiveWrite" Text="0 MB/s" FontSize="32" Foreground="White" FontWeight="Bold"/>
                    </StackPanel>
                    <StackPanel HorizontalAlignment="Center">
                        <TextBlock Text="ANLIK OKUMA" Foreground="#95a5a6" FontSize="10" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtLiveRead" Text="0 MB/s" FontSize="32" Foreground="#FFAA00" FontWeight="Bold"/>
                    </StackPanel>
                </UniformGrid>
            </Border>

            <Border Background="#1a1a1a" CornerRadius="10" Margin="0,20,0,0" Padding="15" BorderBrush="#333" BorderThickness="1">
                <StackPanel>
                    <TextBlock Text="TEST SONUCU (10GB)" Foreground="DimGray" FontSize="11" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                    <UniformGrid Columns="2">
                        <StackPanel>
                            <TextBlock Text="YAZMA SONUCU" Foreground="#7f8c8d" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtFinalWrite" Text="-- MB/s" Foreground="White" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                        <StackPanel>
                            <TextBlock Text="OKUMA SONUCU" Foreground="#7f8c8d" FontSize="10" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtFinalRead" Text="-- MB/s" Foreground="#FFAA00" FontSize="18" FontWeight="Bold" HorizontalAlignment="Center"/>
                        </StackPanel>
                    </UniformGrid>
                </StackPanel>
            </Border>
            
            <StackPanel Margin="0,30">
                <Button x:Name="BtnStartTest" Content="10GB TESTİ BAŞLAT" Click="BtnStartTest_Click" Height="40" Background="#27ae60" Style="{StaticResource ModernButton}"/>
                <ProgressBar x:Name="PbTestProgress" Height="10" Minimum="0" Maximum="100" Background="#333" Foreground="#FFAA00" BorderThickness="0"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</UserControl>
