<UserControl x:Class="HardwareMonitor.CpuStressTest"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="600" d:DesignWidth="600"
             UseLayoutRounding="True"
             TextOptions.TextFormattingMode="Display"
             TextOptions.TextRenderingMode="ClearType">

    <UserControl.Resources>
        <Style x:Key="ModernButton" TargetType="Button">
            <Setter Property="Background" Value="#27ae60"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>

    <Grid Background="#121212">
        <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled">
            <StackPanel Margin="20">
                <TextBlock Text="CPU STRES TESTİ" Foreground="#3498db" FontSize="24" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <Border Background="#1e1e1e" CornerRadius="15" Padding="20">
                    <UniformGrid Columns="2">
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="İŞLEMCİ ISI" Foreground="#95a5a6" FontSize="12" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtCpuTemp" Text="--°C" FontSize="42" Foreground="White" FontWeight="Bold"/>
                        </StackPanel>
                        <StackPanel HorizontalAlignment="Center">
                            <TextBlock Text="İŞLEMCİ YÜKÜ" Foreground="#95a5a6" FontSize="12" HorizontalAlignment="Center"/>
                            <TextBlock x:Name="TxtCpuLoad" Text="%--" FontSize="42" Foreground="#3498db" FontWeight="Bold"/>
                        </StackPanel>
                    </UniformGrid>
                </Border>

                <StackPanel Margin="0,20">
                    <Grid>
                        <TextBlock Text="Test İlerlemesi" Foreground="#7f8c8d" FontSize="12" HorizontalAlignment="Left"/>
                        <TextBlock x:Name="TxtCpuTimer" Text="45s" Foreground="White" FontWeight="Bold" HorizontalAlignment="Right"/>
                    </Grid>
                    <ProgressBar x:Name="CpuProgressBar" Height="10" Minimum="0" Maximum="45" Background="#333" Foreground="#3498db" Margin="0,5"/>
                </StackPanel>

                <Border x:Name="CpuResultCard" Visibility="Collapsed" Background="#1c2833" BorderBrush="#34495e" BorderThickness="1" CornerRadius="12" Padding="20" Margin="0,0,0,20">
                    <StackPanel VerticalAlignment="Center">
                        <TextBlock Text="TEST RAPORU" Foreground="Gold" FontSize="16" FontWeight="Bold" HorizontalAlignment="Center" Margin="0,0,0,10"/>
                        <TextBlock x:Name="TxtCpuNameResult" Foreground="White" FontSize="14" HorizontalAlignment="Center"/>
                        <TextBlock x:Name="TxtCpuStatsResult" Foreground="#bdc3c7" FontSize="13" HorizontalAlignment="Center" Margin="0,5"/>
                        <Separator Background="#34495e" Margin="20,10"/>
                        <TextBlock x:Name="TxtCpuHealthResult" FontSize="20" FontWeight="ExtraBold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <Grid Margin="0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="15"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="BtnStart" Content="TESTİ BAŞLAT" Click="BtnStart_Click" Height="50" FontSize="16" FontWeight="Bold" Style="{StaticResource ModernButton}"/>
                    <Button x:Name="BtnStop" Grid.Column="2" Content="TESTİ DURDUR" Click="BtnStop_Click" Height="50" FontSize="16" FontWeight="Bold" Background="#c0392b" Style="{StaticResource ModernButton}" IsEnabled="False"/>
                </Grid>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
