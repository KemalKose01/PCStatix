<Window x:Class="HardwareMonitor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:HardwareMonitor"
        Title="Hardware Monitor" Height="720" Width="1050"
        MinWidth="900" MinHeight="600"
        WindowStyle="None" AllowsTransparency="True" ResizeMode="CanResize"
        Background="Transparent">

    <Window.Resources>
        <Style x:Key="HiddenScroll" TargetType="ScrollViewer">
            <Setter Property="VerticalScrollBarVisibility" Value="Hidden"/>
            <Setter Property="HorizontalScrollBarVisibility" Value="Disabled"/>
        </Style>

        <Style TargetType="TabItem">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TabItem">
                        <Grid Margin="5,0">
                            <Border x:Name="Border" Padding="25,10" CornerRadius="10" Background="#1A1A1A" BorderBrush="#333" BorderThickness="1">
                                <ContentPresenter VerticalAlignment="Center" HorizontalAlignment="Center" ContentSource="Header" TextBlock.FontWeight="SemiBold" TextBlock.FontSize="13"/>
                            </Border>
                        </Grid>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="Border" Property="Background">
                                    <Setter.Value>
                                        <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                            <GradientStop Color="#00C6FF" Offset="0"/>
                                            <GradientStop Color="#0072FF" Offset="1"/>
                                        </LinearGradientBrush>
                                    </Setter.Value>
                                </Setter>
                                <Setter TargetName="Border" Property="BorderThickness" Value="0"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="Border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#0072FF" BlurRadius="15" Opacity="0.6" ShadowDepth="0"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsSelected" Value="False">
                                <Setter Property="Foreground" Value="#888"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="Border" Property="BorderBrush" Value="#666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Border CornerRadius="15" BorderThickness="2" Margin="10">
        <Border.BorderBrush>
            <LinearGradientBrush StartPoint="0,0" EndPoint="1,0">
                <GradientStop Color="#00E5FF" Offset="0"/>
                <GradientStop Color="#9D50BB" Offset="0.5"/>
                <GradientStop Color="#00E5FF" Offset="1"/>
            </LinearGradientBrush>
        </Border.BorderBrush>
        <Border.Background>
            <SolidColorBrush Color="#090909" Opacity="0.96"/>
        </Border.Background>
        <Border.Effect>
            <DropShadowEffect Color="Black" BlurRadius="30" ShadowDepth="10" Opacity="0.8"/>
        </Border.Effect>

        <Grid>
            <Grid Background="Transparent" Height="60" VerticalAlignment="Top" MouseLeftButtonDown="TopBar_MouseLeftButtonDown">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center">
                    <Border Width="4" Height="24" Background="#00E5FF" CornerRadius="2" Margin="0,0,10,0">
                        <Border.Effect>
                            <DropShadowEffect Color="#00E5FF" BlurRadius="10"/>
                        </Border.Effect>
                    </Border>
                    <TextBlock Text="PC" Foreground="White" FontWeight="Light" FontSize="22"/>
                    <TextBlock Text="STATIX" Foreground="#00E5FF" FontWeight="Bold" FontSize="22" Margin="5,0,0,0">
                        <TextBlock.Effect>
                            <DropShadowEffect Color="#00E5FF" BlurRadius="20" ShadowDepth="0" Opacity="0.5"/>
                        </TextBlock.Effect>
                    </TextBlock>
                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,15,0">
                    <Button Content="─" Width="40" Height="40" Click="Minimize_Click" Background="Transparent" Foreground="#888" BorderThickness="0" FontSize="18" Cursor="Hand"/>
                    <Button Content="▢" Width="40" Height="40" Click="Maximize_Click" Background="Transparent" Foreground="#888" BorderThickness="0" FontSize="18" Cursor="Hand"/>
                    <Button Content="✕" Width="40" Height="40" Click="Close_Click" Background="Transparent" Foreground="#FF5555" BorderThickness="0" FontSize="18" FontWeight="Bold" Cursor="Hand"/>
                </StackPanel>
            </Grid>

            <Grid Margin="0,60,0,0">
                <TabControl Background="Transparent" BorderThickness="0" Margin="0,0,10,0">
                    <TabControl.Resources>
                        <Style TargetType="TabPanel">
                            <Setter Property="HorizontalAlignment" Value="Center"/>
                            <Setter Property="Margin" Value="0,0,0,15"/>
                        </Style>
                    </TabControl.Resources>

                    <TabItem Header="BİLGİ İZLEME">
                        <Grid Margin="20,10,20,20">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="1.2*"/>
                                <ColumnDefinition Width="20"/>
                                <ColumnDefinition Width="1*"/>
                            </Grid.ColumnDefinitions>

                            <Border Grid.Column="0" CornerRadius="15" Padding="25" BorderThickness="1" BorderBrush="#222">
                                <Border.Background>
                                    <LinearGradientBrush StartPoint="0,0" EndPoint="1,1">
                                        <GradientStop Color="#141414" Offset="0"/>
                                        <GradientStop Color="#0F0F0F" Offset="1"/>
                                    </LinearGradientBrush>
                                </Border.Background>
                                <Grid>
                                    <Grid.RowDefinitions>
                                        <RowDefinition Height="Auto"/>
                                        <RowDefinition Height="*"/>
                                    </Grid.RowDefinitions>

                                    <StackPanel Grid.Row="0">
                                        <Grid Margin="0,0,0,10">
                                            <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                                <TextBlock Text="⚡" Foreground="#FF4444" FontSize="18" Margin="0,0,10,0"/>
                                                <TextBlock Text="İŞLEMCİ (CPU)" Foreground="#00E5FF" FontWeight="Bold" FontSize="15" Margin="0,0,0,10">
                                                    <TextBlock.Effect>
                                                        <DropShadowEffect Color="#00E5FF" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
                                                    </TextBlock.Effect>
                                                </TextBlock>
                                            </StackPanel>
                                            <TextBlock x:Name="TxtCpuTemp" Text="--°C" Foreground="#FF4444" FontSize="24" FontWeight="Bold" HorizontalAlignment="Right">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="#FF4444" BlurRadius="15" Opacity="0.4"/>
                                                </TextBlock.Effect>
                                            </TextBlock>
                                        </Grid>
                                        <TextBlock x:Name="TxtCpuName" Text="CPU Modeli Bekleniyor..." Foreground="#DDD" Opacity="0.7" FontSize="13" Margin="0,0,0,15"/>
                                    </StackPanel>

                                    <ScrollViewer Grid.Row="1" Style="{StaticResource HiddenScroll}">
                                        <ItemsControl x:Name="IcCpuCores">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Border Background="#1F1F1F" CornerRadius="6" Margin="0,4" Padding="12,8">
                                                        <Grid>
                                                            <Grid.ColumnDefinitions>
                                                                <ColumnDefinition Width="1.5*"/>
                                                                <ColumnDefinition Width="2*"/>
                                                                <ColumnDefinition Width="1.2*"/>
                                                            </Grid.ColumnDefinitions>
                                                            <TextBlock Text="{Binding Name}" Foreground="White" FontSize="13" VerticalAlignment="Center" FontWeight="SemiBold"/>
                                                            <ProgressBar Grid.Column="1" Value="{Binding LoadValue}" Maximum="100" Height="8" Background="#333" Foreground="#00E5FF" BorderThickness="0" Margin="10,0,10,0" VerticalAlignment="Center">
                                                                <ProgressBar.Clip>
                                                                    <RectangleGeometry Rect="0,0,200,8" RadiusX="4" RadiusY="4"/>
                                                                </ProgressBar.Clip>
                                                            </ProgressBar>
                                                            <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right">
                                                                <TextBlock Text="{Binding Load}" Foreground="#00E5FF" FontSize="13" FontWeight="Bold" Margin="0,0,8,0"/>
                                                                <TextBlock Text="{Binding Clock}" Foreground="#AAA" FontSize="12" VerticalAlignment="Center"/>
                                                            </StackPanel>
                                                        </Grid>
                                                    </Border>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </ScrollViewer>
                                </Grid>
                            </Border>

                            <StackPanel Grid.Column="2">
                                <Border CornerRadius="15" Margin="0,0,0,15" Padding="20" BorderThickness="1" BorderBrush="#222" Background="#141414">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                                            <TextBlock Text="🎮" Foreground="#9D50BB" FontSize="16" Margin="0,0,10,0"/>
                                            <TextBlock Text="GRAFİK (GPU)" Foreground="#9D50BB" FontWeight="Bold" FontSize="14">
                                                <TextBlock.Effect>
                                                    <DropShadowEffect Color="#9D50BB" BlurRadius="10" ShadowDepth="0" Opacity="0.4"/>
                                                </TextBlock.Effect>
                                            </TextBlock>
                                        </StackPanel>

                                        <TextBlock x:Name="TxtDGpuName" Text="Harici Ekran Kartı" Foreground="White" FontSize="12" Opacity="0.8" Margin="0,0,0,15" TextWrapping="Wrap" MinHeight="32"/>

                                        <Grid>
                                            <Grid.ColumnDefinitions>
                                                <ColumnDefinition Width="*"/>
                                                <ColumnDefinition Width="10"/>
                                                <ColumnDefinition Width="*"/>
                                            </Grid.ColumnDefinitions>

                                            <Border Grid.Column="0" Background="#1A1405" BorderBrush="#332200" BorderThickness="1" CornerRadius="12" Padding="8">
                                                <StackPanel HorizontalAlignment="Center">
                                                    <TextBlock Text="SICAKLIK" Foreground="#AA8800" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                    <Viewbox Stretch="Uniform" MaxHeight="32">
                                                        <TextBlock x:Name="TxtDGpuTemp" Text="--°C" Foreground="#FFCC00" FontSize="22" FontWeight="Bold">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="#FFCC00" BlurRadius="10" Opacity="0.2"/>
                                                            </TextBlock.Effect>
                                                        </TextBlock>
                                                    </Viewbox>
                                                </StackPanel>
                                            </Border>

                                            <Border Grid.Column="2" Background="#14051A" BorderBrush="#2A0033" BorderThickness="1" CornerRadius="12" Padding="8">
                                                <StackPanel HorizontalAlignment="Center">
                                                    <TextBlock Text="KULLANIM" Foreground="#9D50BB" FontSize="10" FontWeight="Bold" HorizontalAlignment="Center"/>
                                                    <Viewbox Stretch="Uniform" MaxHeight="32">
                                                        <TextBlock x:Name="TxtDGpuLoad" Text="%--" Foreground="#C77DFF" FontSize="22" FontWeight="Bold">
                                                            <TextBlock.Effect>
                                                                <DropShadowEffect Color="#C77DFF" BlurRadius="10" Opacity="0.2"/>
                                                            </TextBlock.Effect>
                                                        </TextBlock>
                                                    </Viewbox>
                                                </StackPanel>
                                            </Border>
                                        </Grid>

                                        <Border Margin="0,12,0,0" Padding="10" Background="#0F0F0F" CornerRadius="8" BorderThickness="1" BorderBrush="#222">
                                            <Grid>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                                    <TextBlock Text="iGPU" Foreground="#666" FontWeight="Bold" FontSize="11" Margin="0,0,8,0"/>
                                                    <TextBlock x:Name="TxtIGpuName" Text="Integrated" Foreground="#888" FontSize="11" MaxWidth="100" TextTrimming="CharacterEllipsis"/>
                                                </StackPanel>
                                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                                    <TextBlock Text="Yük:" Foreground="#666" FontSize="11" Margin="0,0,5,0"/>
                                                    <TextBlock x:Name="TxtIGpuLoad" Text="%--" Foreground="#00E5FF" FontSize="11" FontWeight="Bold"/>
                                                </StackPanel>
                                            </Grid>
                                        </Border>
                                    </StackPanel>
                                </Border>

                                <Border CornerRadius="15" Margin="0,0,0,15" Padding="20" BorderThickness="1" BorderBrush="#222" Background="#141414">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="💾" Foreground="#00E5FF" FontSize="18" Margin="0,0,10,0"/>
                                            <TextBlock Text="RAM BELLEK" Foreground="#00E5FF" FontWeight="Bold" FontSize="14"/>
                                        </StackPanel>
                                        <Grid Margin="0,0,0,5">
                                            <TextBlock Text="Kullanılan:" Foreground="#AAA" FontSize="11" HorizontalAlignment="Left"/>
                                            <TextBlock x:Name="TxtRamInfo" Text="-- / -- GB" Foreground="White" HorizontalAlignment="Right" FontSize="12" FontWeight="Bold"/>
                                        </Grid>
                                        <ProgressBar x:Name="PbRam" Height="6" Maximum="100" Background="#222" Foreground="#00E5FF" BorderThickness="0">
                                            <ProgressBar.Clip>
                                                <RectangleGeometry Rect="0,0,500,6" RadiusX="3" RadiusY="3"/>
                                            </ProgressBar.Clip>
                                        </ProgressBar>
                                    </StackPanel>
                                </Border>

                                <Border CornerRadius="15" Padding="20" BorderThickness="1" BorderBrush="#222" Background="#141414">
                                    <StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                                            <TextBlock Text="💽" Foreground="#FFAA00" FontSize="18" Margin="0,0,10,0"/>
                                            <TextBlock Text="DİSK (C:)" Foreground="#FFAA00" FontWeight="Bold" FontSize="14"/>
                                        </StackPanel>
                                        <Grid Margin="0,0,0,5">
                                            <TextBlock Text="Doluluk:" Foreground="#AAA" FontSize="11" HorizontalAlignment="Left"/>
                                            <TextBlock x:Name="TxtDiskInfo" Text="-- / -- GB" Foreground="White" HorizontalAlignment="Right" FontSize="12" FontWeight="Bold"/>
                                        </Grid>
                                        <ProgressBar x:Name="PbDisk" Height="6" Maximum="100" Background="#222" Foreground="#FFAA00" BorderThickness="0">
                                            <ProgressBar.Clip>
                                                <RectangleGeometry Rect="0,0,500,6" RadiusX="3" RadiusY="3"/>
                                            </ProgressBar.Clip>
                                        </ProgressBar>
                                    </StackPanel>
                                </Border>
                            </StackPanel>
                        </Grid>
                    </TabItem>

                    <TabItem Header="GPU TEST">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                            <local:GpuStressTest Width="600" Height="450"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="CPU TEST">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                            <local:CpuStressTest Width="600" Height="450"/>
                        </Grid>
                    </TabItem>
                    <TabItem Header="DISK TEST">
                        <Grid VerticalAlignment="Center" HorizontalAlignment="Center">
                            <local:DiskStressTest Width="600" Height="450"/>
                        </Grid>
                    </TabItem>
                </TabControl>
            </Grid>
        </Grid>
    </Border>
</Window>
